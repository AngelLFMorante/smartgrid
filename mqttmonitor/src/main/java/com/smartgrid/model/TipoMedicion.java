package com.smartgrid.model;

/**
 * Enum que define los tipos de mediciones energéticas soportadas por el sistema.
 */
public enum TipoMedicion {
    VOLTAJE,
    CORRIENTE,
    FRECUENCIA,
    POTENCIA
}
